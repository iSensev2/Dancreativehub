// Function to add item to cart
function addToCart(itemName, price) {
    // Here you can implement your logic to add items to cart
    console.log(`Added ${itemName} to cart with price $${price}`);
    alert(`Added ${itemName} to cart!`);
}
